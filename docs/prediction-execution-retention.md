# 私有执行证据：有界长期留存与可见容量缺口

## 已修复的限制

原目录上限为 256 MiB / 512 批。已保存的一批真实 Linux 新计算（34 条记录）压缩后 767,824 字节；若每五分钟保存同样大小的一批，原字节上限约 349 批、29.1 小时就先耗尽，不能只看文件数上限对应的 42.7 小时。这是样本大小外推，不是生产已经连续运行这么久的观测。

本次保持原内容寻址 `.json.gz` 文件格式、原始内容哈希、完整输入输出、每记录/每批大小上限和独占锁，修改存储预算与可观测性：

- 目录最大 **48 GiB / 60,000 批**，不预分配磁盘、不创建付费资源；旧文件原位保留，不迁移、重压缩、覆盖或自动删除。
- 每次新写入前读取实际文件系统可用空间，检查写后仍有 **8 GiB 余量**；无法取得可靠空间信息或预算不足则停止新增，保留原模型计算返回值和旧批次。
- 这是写前观察保护，不是独占磁盘预留；其他服务仍可能同时用盘。真正 ENOSPC/写入失败继续返回错误，不冒充成功，不删旧证据释放空间。
- 返回实际文件数/压缩字节、上限、当前批次大小和剩余可容纳批数；按“同样压缩大小、每五分钟一批”估算剩余天数，少于 30 天标 watch，耗尽标 full。估算明确不是保留时长保证。
- 相同已保存批次先验证内容，再按 duplicate 返回；即使此刻空间不足或无法读取剩余空间，也不把已经验证留存的文件说成未保存，但容量状态不能是健康。

沿用平面目录，仍在同一独占锁中逐文件核算，拒绝未知文件/链接/异常容量；没有用一份可能失真的累计计数替代实际磁盘文件清单。60,000 文件量级的核算开销另做了真实 Linux 测量，见下文。

以那一批 767,824 字节为假设，180 天、每五分钟一批约需 51,840 个文件、37.1 GiB；新文件上限约对应 208.3 天。实际批次变大、频率提高、其他服务用盘都会缩短时间，因此不能承诺“已经解决所有六窗留存”或“保证 208 天”。

## 真实接入位置与隐私

实际 `syncData` 仍调用 `predictionExecutionCapture.persist(DEFAULT_STORE_DIR)` 并把结果写入 sync-meta，本次直接扩展该返回值。批次正文不加入动态容量字段，已有批次内容哈希不因剩余磁盘改变。

`GET /api/v1/health` 新增 `storage.predictionExecutionCapture`，从实际健康元数据中的捕获结果生成白名单聚合字段。它重新计算容量状态，不信任输入的 `status:ok`；无捕获、旧版无容量信息、超过 30 分钟的容量观察、未来时间和失败都明确表现为缺口。完整记录、输入输出、原始哈希、路径和任意额外字段不进入该公开状态。

`checkServerRuntime` 的实际健康检查接入 `prediction execution evidence storage` 项，缺能力为 watch、预算不足或捕获失败为 failed。不修改推荐可靠性算法、不自动重启服务、不自动清理文件、不把私有捕获转为正式或已验证来源；`productionEligible=false`、`sourceVerified=false` 保留。

## 验证

1. 既有捕获验证器新增 18 项，总 40 项，在 Windows/Linux Node22.22.1 均通过。实际创建 **513 个不同批次**，逐个验证 gzip 解压内容哈希，末尾再检查所有早期文件字节没有变化；不是伪造文件计数。覆盖文件/字节/可用空间边界、低余量告警、非法 statfs、未知/过期/未来观察、重复留存和公开诊断字段白名单。没有分配 48 GiB 测试数据。
2. 独立实际 SQLite → 元数据 → HTTP 验证总 278 项通过：测试元数据故意写 `status:ok`、文件数 60,000 和私有 canary，真实健康接口正确返回容量失败及文件数，未暴露 canary，正式权限仍 false。该套测试的 PG 是 query-capture transport，不能当作本轮真实 PG/生产验证。
3. 原运行监控 59 项保留，另执行真实健康到监控源码片段的 11 条断言，覆盖 ok/watch/failed/缺失/未知状态。13 项独立进程重放、14 项全输出时钟和冻结归档权限回归通过。生产 readiness 现在明确要求 40 项、18 留存项、513 实存批次和容量策略 v2；发布合同新增 3 项拒绝过旧/不足留存证明的反例，总 88 项。

03:17:57 服务器只读查询生产目录所在文件系统，可用 **120,697,204,736 字节**。使用独立 `/var/tmp/football-replay-qCvdSiHE` 目录中的候选源运行 40 项测试，不安装依赖、不写生产目录。候选捕获模块 SHA `5ae3bd73e2f12d35f1cd50a216f79bb5b40568b71625c6f63efe7c13e1bbc52e`；原隔离基线模块字节保持不变。

03:18:55–03:19:01，在另一个独立测试目录建立 **60,000 个明确标为合成元数据的压缩文件**，总压缩内容 5,307,615 字节，创建约 4.27 秒。真实持久化入口两次完整扫描分别 **790.5 / 356.0 ms**，均正确拒绝第 60,001 批，核算字节精确，所有原文件名保留。它验证大目录核算和容量拒绝，不是 60,000 个真实预测、长期实跑或全库内容审计；本次缓存/负载下的耗时不作为线上硬性 SLA。目录暂保留用于复核，实际块/元数据占用多于压缩内容字节。

私有回取报告：`outputs/capture-capacity-linux-20260908.json` SHA `4c732b8710f06b1501ee5bcc728798f8adbde284993c8784a84cabc26c45fd50`；`outputs/capture-capacity-scan-20260908.json` SHA `1980463f821c9815a697f522daf94cc83a28fe092e08cf1c62e2708f9858eaed`。测试数据不上传 Git。

## 仍未完成的范围

这不是跨主机备份、完整实际加载依赖闭包或六窗独立前瞻完成证明。旧式空文件锁仍不能自动恢复；新目录锁恢复范围见下节。存储模块实现身份已改变，旧捕获只能按原代码身份验证，不能拿旧批次替新实现证明严格重放。预测公式、历史公开方向、冻结版本和晋级门槛未改变，不能声称命中率提高。

本轮不修改冻结 6f86 的 r702 签名包或单次队列。新能力尚需后续受控发布与真实 worker 周期确认；本地/隔离通过不等于已上线。Q1–Q5 的其余范围和新官方赛果真正写入验收继续保留。

## 2026-09-08：写锁异常退出保护与有界释放

空文件 `wx` 锁无法证明异常退出后的归属。本次改用既有 `dataGenerationStore.acquirePointerCommitLock`：新锁是目录，元数据含同主机 PID、随机 token、取得时间；释放/恢复核对精确 inode、owner 内容和 token，先隔离再清理。只自动恢复能够确认已经退出的同主机完整 owner；活跃 owner、异地主机、残缺元数据、额外条目、旧式空文件均保留并返回 busy，不按锁年龄删除。

初次接入的真实 513 批测试失败，保留失败现场。进一步诊断观察到 Windows `renameSync(lockDir, quarantinePath)` 偶发 `EPERM`，返回 `POINTER_LOCK_RELEASE_FAILED/quarantine-failed`。现在只对 `claim-raced`、`quarantine-failed` 两种可重新检查的状态最多调用四次 release，每次仍由原实现完整核对归属，重试间隔 20ms（最多三个间隔，不含文件操作耗时）。不做 canonical unlink、递归删除兜底或无限重试。

释放最终失败不改变已成功留存的事实，返回 `lockReleaseFailed=true`，健康状态为 `failed/capture-writer-lock-release-not-proven`；诊断失败不能让已完成的公共同步抛错。部分写入和不可证明的锁保持 fail-closed，仍需人工核查。公式、记录正文编码、历史方向和正式权限不变。

验证新增 9 项，总 49 项，Windows 和 Linux Node22.22.1 各通过：真实已退出子进程的锁恢复且旧 gzip 字节不变、旧时间的活跃 owner、外主机、残缺/额外元数据、旧空文件、有界临时/永久重命名拒绝，以及实际多进程 ABA/释放时替换 owner 的 10 断言子验证。仍实际新增并回读 513 批，不只模拟计数。Windows 七套回归通过：49 捕获、278 SQLite/实际 HTTP（PG transport 替身）、13 独立重放、14 时钟、59+11 监控、92 发布合同、10 冻结归档；七个改动脚本 lint 通过。

readiness 必须报告至少 49 项、9 项锁验证以及原 18 留存项/513 批；打包和验包的 required entries 都新增实际多进程锁验证器，缺失即拒绝。它随捕获验证器真实执行，不仅检查文件存在。

Linux 使用独立 `/var/tmp/football-replay-tJVmbXwG`，普通 ubuntu 身份、低优先级、512MiB Node 堆上限、90 秒子进程期限；无依赖安装或生产目录写入。03:48:51–03:49:01 的复验前后 499 个候选文件哈希不变。捕获源 SHA `974d6309294520d4add403eb2b35c541c81b93425be972057e75a04651be464c`，测试 SHA `4524ceac785b686be8b6ab50a2e782961753630ec226c3ebeca76ea13a39827f`，报告 SHA `d619a8cfb1b75af50fad2a720c9c710abe2404249c8ddebbc3debb7deaf21262`。文件清单校验不是完整实际加载依赖闭包证明。

本地私有报告 `outputs/capture-lock-regressions-20260908.json`；Linux `capture-lock-verification.json` 在上述隔离根目录。测试现场保留，ABA 子测试仅清理自己生成并核对 realpath 的临时目录；不涉及生产数据。新代码不在已冻结并正在运行的 r702 中，仍待后续发布验收。

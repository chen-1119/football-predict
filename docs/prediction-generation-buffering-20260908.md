# 生成链路缓冲优化与验收（2026-09-08）

本补丁属于后续候选，不在已签名、排队中的 r704 中。不得修改 r704 的冻结源码、签名或队列来补入此改动。本文记录代码与隔离测试结果，不代表已部署或命中率提高。

## 现场与范围

旧 r699 的 05:00:33–05:06:57 同步轮次耗时 384436 ms。官方生成阶段 107955 ms，SQLite/PG 导出阶段 106412 ms，合计约占整轮 56%。这只是阶段定位；尚未证明服务器全部慢因，也未把本地提速直接换算成线上耗时。

改动仅涉及数据生成的 CPU/分配开销：

- 规范化语义摘要的完整 JSON token 按最多 64 Ki UTF-16 单元、2048 片段合并后调用 SHA-256；超大单 token 直接处理。
- 历史数组解析仍使用原有 UTF-8 解码器、转义和嵌套状态、逐行 JSON.parse、唯一 ID 及原始字节摘要，但保留块切片，不再逐字符拼接。
- 没有修改规范化字段、数据来源、推荐方向、截止锁、时效阈值、正式模型晋级条件或发布写锁。

## 完整数据对照

Windows Node 22.22.1，原版/候选/候选/原版（ABBA）独立进程。原版取 r704 冻结树，输入同样只读取自冻结树，不是当前线上实时输入。

| 测试阶段 | 原版两次耗时 ms | 候选两次耗时 ms | 两次均值下降 |
| --- | --- | --- | --- |
| 七个核心文件语义摘要 | 2586.66 / 2528.73 | 1714.01 / 1707.42 | 约 33% |
| 历史扫描，不计算语义摘要 | 1678.07 / 1717.87 | 627.54 / 637.08 | 约 63% |
| 完整首次生成 | 8033.40 / 7724.84 | 5419.75 / 5352.53 | 约 32% |
| 内容未变的完整生成校验 | 9340.44 / 9309.35 | 6400.23 / 6701.54 | 约 30% |

完整生成对照包含 11 文件、194956674 字节、39 当前比赛、2957 历史比赛、3812 预测行。每个进程使用独立临时存储，固定测试 committedAt 仅用于比较相同发布身份，不回填生产时钟。首次生成和第二次 semanticNoop 的 pointer、manifest、validation、semanticHash 在四次运行间完整一致；输入不变，测试存储核对真实路径后清理。峰值 RSS 约 301–302 MiB，未观测到这组测试的内存降低。

语义摘要均为 `6aaa962a28030e10f31dbdf54edbf756ed1f5845ed6f6bdb9a700d10314023ac`。语义阶段原生 hash.update 次数从 16971732 降为 8287。上述为局部/本地测量，不是部署总时间或 Linux 完整轮次性能承诺。

## 回归与发布门禁

- 完整端到端 227 项通过，其中新增 125 项覆盖空数组、跨 64 KiB 的中文/表情/转义、520 多块记录、重复/缺失 ID、坏 JSON、无效标量及独立旧规范化摘要兼容性。
- 事务回归 89 项通过。端到端包含真实 SQLite/HTTP，PG 切换场景采用测试替身；此前 r704 的真实 PG 390 项不当作本补丁的新测试结果。
- 发布检查调用 `verifyDataGenerationEndToEnd.cjs --buffered-compatibility-only`，要求精确版本、125 项兼容性断言、生产数据未触碰。完整 227 项保留，不在每次部署重复较慢的切换演练。
- 新验证器同时加入打包与签名验包必需文件；发布合同 110 项通过，包含失败退出、缺少断言、错误版本、生产写入证据的拒绝测试。部署配置 122 项、eslint、diff 检查及构建通过。
- Linux Node 22 非 root、只读源码目录实际运行专门回归：125 项通过、364 ms、497 输入文件摘要前后不变。只读写探测实际 EACCES，512 MiB 堆限制、低优先级、30 秒上限，生产写入 0。

本次构建仍提示旧有 `/media/worldcup-glory-hero.jpg` 运行时解析警告；不冒充已修复该 UI 资源。

## 私有证据

工作树 outputs 中保留（不进公开 Git）：

- `generation-stage-comparison-1788816108773.json`，SHA-256 `3ee0c1d17b354144106958655fa49bb6b95f793e64efedeba6372e26faa8cd07`。
- `generation-commit-comparison-1788816582849.json`，SHA-256 `d8418003ab67bb27fec39b6c1f3c5b19d887e8820a52eff8173829388c638438`。
- `buffered-generation-linux-report-20260908.json`，SHA-256 `d67ea6fcfdf013d995365f0454ed22856d8105c1fadbbe38e1fd16655d2512a0`。

Linux 隔离目录 `/var/tmp/football-buffered-generation-uJezN6Fe` 保留证据；不是生产应用目录。完整输入/结果未向新第三方服务披露。

## 尚未完成

本补丁尚未单独签名部署，也没有新 Linux 完整生产轮次耗时。r704 应按原队列完成原身份的验收，后续补丁再独立评估。预测原方向争议、正式统计、补充源质量、前瞻模型验证仍须按 Q1–Q5 的原验收条件推进，不能以提速测试代替。

# 历史评估入口与结果时间水位校验

## 发现的真实代码行为

2026-09-07用实际normalizeHistoricalEvent构造合成输入：scoreHome=null、scoreAway=false、availableAt=2026-02-30T12:00:00Z，原入口接受并产生0:0/X标签，时间被规范化为2026-03-02T12:00:00.000Z。新增负面测试先在原代码失败，再实施修复。这不是发现线上已有该坏记录的声明，也不能据此解释全部线上命中率。

## 本次修复

- 归一化历史比分必须是真正的非负安全整数；null、布尔值、空串、数字字符串、数组/对象、小数、负数、非有限数和越界数不能转为进球数。
- 结果可用时间复用严格带时区日历解析；非法日历/24:00/无时区/日期值/非字符串拒绝。显式无效的优先字段不能通过备用字段悄悄得到有效时间或比分；只有undefined表示字段缺席。
- 特征制品校验重新走同一标签归一化入口，核对比分、赛果方向、规范日期/时间和事件承诺。重新计算所有内容哈希不能让语义无效的标签通过。
- 根据所有合法标签重新构造完整源日期批次，按availableAt顺序推进；每条预测的历史结果行数、批数、批次根哈希、最晚已知时间/比赛日期与最终水位必须准确一致。它校验时间水位与标签语义，不替代任意模型实现/最终状态的独立数学重放证明。
- 固定A/B/C研究运行前后实现承诺增加真实strictInstant依赖；新研究报告外层哈希与旧报告分开，保留旧负面结果。没有改变模型权重、日历划分、候选网格、筛选条件、正式晋级门槛或生产冻结记录。

## 已执行验证

49项专项检查通过，并接入原verifyHistoricalStrengthModel入口；原33项强度模型、16项历史仓库、历史市场验证、19项固定A/B/C、9项消融与定向ESLint通过。49项包括保持修复前的合法五场模型/特征/最终状态承诺、null/false等比分、坏时间/回退，以及重新计算哈希后的错赛果、坏时间和伪造时间水位。

从现有只读仓库230,554行中按原固定日历选取原来的20,000场完整重跑：source全部输入/模型/特征承诺、research整个报告、ablations整个报告与combinedConclusion逐对象deepEqual完全一致；外层新旧manifest均独立重算核对。

- 共同概率对照3,636场，共同可判定3,624场。A/C各1,840命中（50.77%），B共同组1,777（49.03%），C模型残差权重仍0。所有概率、误差区间与消融结果不变，不是改了分母让命中率变高。
- 旧文件408,433字节，文件SHA256=0f22cb477570d8ea6480130fd46160fd170ca0e12c514c7d1ed163bad3875875；其JSON内部manifestHash=6a8de239dcdc2d5f9f318f2f1b203d65ec22ec949f2938d151e15b778cc8f74d。文件字节哈希和内部清单哈希是不同值。
- 新文件outputs/fixed-abc-admission-audit-20260907.json，408,542字节，文件SHA256=e763c4e181c2c8f4006334a1a7bf9ae73452925914f8226a03eb7bf1881d4c67；内部manifestHash=79402bcd1797a619eb8fa78a44298a33e8ef1b3a97fd76f8572e0f166482818a。差异仅为运行器、归一化校验实现哈希和新增严格时钟依赖记录。
- 原动态模型制品仍c7d5e5862615d290ada272a9e7a7d81193f5898946c5884151c9209e66513022。固定研究仍productionEligible=false/nominationAllowed=false，历史结果可用时间仍可能是推导值，不冒充真实源观测或独立盲测。

## 发布边界

这是独立UI集成工作树的新研究修复，不在22:03等待尝试的r701中，不改其源码、包或队列。完整Q1–Q5仍未完成；真实源时钟/近期数据、映射采纳、独立前瞻与最终发布验收继续保留。

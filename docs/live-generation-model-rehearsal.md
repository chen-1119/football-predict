# 线上发布代副本：完整模型工件复演

## 本轮结论（2026-09-08 北京时间）

基于源码 `bdb1e21d54daa78b6ca01776fea832f936df7f54`，在独立 detached 工作树完成 SQLite 重建、实际模型回测、策略重建、候选账本连续性和完整计划检查。最后一次复演结束于 `2026-09-07T16:10:27.935Z`：104 项检查中的 99 项必需检查全部通过，失败 0；原独立 UI 工作树中的 10 项失败不能再被描述为已确认代码缺陷，缺失/未重建的本地工件是这次复演消除的差异。

这不是完整 production-readiness、真实 PostgreSQL、完整生产 SQLite 历史克隆或上线验收，也不意味着 Q1–Q5 完成。没有改生产环境、采集密钥、原工作区或 r702 签名源。

## 输入身份和复制边界

- 验证代：`g-e283a2cfc0fd0cbe7fa0c3ac296e670351785b7d05cde5727de8275213c4eb7f`，源周期 `sporttery-full-sync:2026-09-07T15:53:08.179Z`。
- 13 个发布代文件逐一通过字节数与 SHA-256 核对；43 场当前赛事、2,227 场历史、3,952 条公开预测快照、1,801 条赔率状态。
- 另外只读复制三份实际模型账本：候选注册表、基准账本、学习注册表。服务器读取区间为 `16:05:30.304Z–16:05:30.422Z`，读取前后字节一致；此时活动发布代也为上述代。
- 发布代文件与账本分别采集，不冒充跨文件原子快照。先前整体归档因私有文件在传输期间变化被拒绝；其中私有内容完全未采用。首次误用传输前盘点的旧代 `g-65dc…` 也被哈希校验拒绝，最终通过全部 13 文件哈希唯一匹配服务器保留清单，确认真实代身份。
- 首次大小保护拒绝后，只对已盘点为 83,855,659 字节的 G2-v2 文件调整诊断复制上限；没有降低生产签名或完整性门禁。
- SQLite 从验证后的发布 JSON 在本地重新生成，记录为 `legacy-bootstrap`，没有伪造为原生产数据库，也不包含未发布的更多旧 SQLite 快照。Node 为本地 `v25.8.1`，不是生产 Node 22 的运行证明。

## 关键验证配置纠正

首轮本地回测缺少 `SPORTTERY_COLLECTOR_TRUST_REGISTRY_PATH`，因而出现 0 条严格概率样本及注册表缺失拒绝。这个结果不能用来断言生产没有合格原始证据。

第二轮显式使用已存在的公钥注册表；其 SHA-256 为 `a5969ec4039dba8fde6e9d294aaaf9d49937df7c54409581a22457285f04c85a`、4 个公钥条目，与线上完全相同。`16:12:08Z` 只读核对实际 worker PID 3359467 和应用 PID 2968633 的进程环境，两者均已配置该路径及哈希。虽然 `/etc/football-predict/env` 没有此项，服务单元会注入，不能据此判定生产遗漏。未生成/更换密钥，未改变信任对象。

纠正本地配置后，同一批原数据得到 178 条严格概率对照，逐玩法证据 322 条中 50 条通过记录级校验（HAD 20，HHAD 30），272 条拒绝。50 条不是 50 场独立比赛，也不是正式发布/已晋级样本；证据清单自身可用标志不能代替策略晋级门槛。

## 模型负面结果必须保留

| 对照口径 | 同组样本 | 模型命中率 | 赔率基准命中率 | Brier 改善 | Log loss 改善 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 严格赛前概率对照 | 178 | 46.07% | 50.56% | -0.0275 | -0.0385 |
| 旧口径诊断对照 | 393 | 45.29% | 54.20% | -0.0516 | -0.0729 |

两类分母独立，不与正式、实时、参考或其他模型版本混算；这不是线上正式推荐命中率。负的改善值代表模型差于基准。旧口径仅供诊断，不支持晋级；严格对照也尚未证明满足全部来源、输入、前瞻和窗口条件。

策略重建为 `self-optimization-v9-formal-ledger`，`onlineEffect=shadow`。仍有 50<500 的证据数量不足、输入晋级不合格、缺独立前瞻确认、walk-forward 未验证、玩法未验证以及模型无边际优势等阻断。没有用合规样本增加为由激活候选。

## 连续性与完整性

两轮实际回测后，旧候选账本 admitted/atomic=334、settled/formal=324、finalized=524 均保留；这些是账本内字段，不是当前网站正式推荐数量。新实现修订 `@1007d2124017d211` 的对应计数全部为 0，不继承旧激活权限或样本。精确连续性校验通过。

两份 current/history 的冻结归档及独立公开参考记录摘要前后不变；所有捕获输入文件的哈希也保持不变。复制、回测与策略改写仅发生在新隔离目录，输出和私有账本不上传 Git。

本地证据：

- `../prediction-quality-readiness-20260908/outputs/generation-readiness/report.json`（从 `.codex-tmp/prediction-quality-ui-20260907` 定位兄弟工作树）。
- UI `outputs/generation-rehearsal-inputs-Xmi92j/capture-receipt.json`。
- 新隔离树 `outputs/generation-readiness/production-plan-with-registry.stdout.json` 和 `candidate-after-second-run.json`。

## 发布与下一步

`16:12:59Z` 同一个 r702 队列 PID 3577920 仍存活、等待计划时间、尚未尝试；预计北京 9 月 8 日 03:31 单次启动，仍须通过安全门禁。双线上标记仍是 r699，SHA `e4bb349180b3dfce4df305fc8fdb2820868709208ae660b13fb018a825a343ef`。没有重排或重复部署。

后续继续补充真实输入、核对被拒证据的具体合同缺口、固定模型对照/消融与独立前瞻；新改动须通过完整生产候选与上线验收。不能用本次 99 项计划门禁代替原 Q1–Q5、Newcastle 历史公开方向裁定或模型质量目标。

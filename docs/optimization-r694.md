# r694 候选：受限内存读取与复盘玩法统计

状态：候选实现和验证记录，不是上线证明。r693 在切换前预构建超时，旧 r687 已恢复。

## 部署内存修复

r693 的 v3→v5 资格检查已通过，实际升级却整份读取/解析 prediction-snapshots.json。生产文件 428,497,267 字节，预构建限制 MemoryHigh=896MiB、MemoryMax=1GiB、swap=256MiB；观察到约987MB匿名内存、交换配额用满、memory pressure avg10≈91%，最终15分钟超时。

新增严格流式顶层字段读取，只保留 `updatedAt`、`retentionDays`、`publicReferenceDecisions`、`publicReferenceEvidence`。跳过字段仍验证完整 JSON 语法，按原始字节校验整文件大小与 SHA256；前后 inode/文件元数据、路径、符号链接及清单身份继续检查。只有受限 v3→v5 克隆升级使用此读取；正常导出与已完成升级的 no-op 语义不变，不修改现有内存限制、不放宽发布门禁。

保留值使用原生 JSON.parse，包含重复键的最后值和数值语义；非法 UTF-8 拒绝。为保证资源有界，最大深度256、顶层键16Ki字符、标量4096字符、所选字段累计128Mi字符；超出明确拒绝，不截断或省略所需证据。未来选择内容增长仍需监测，不宣称任意大小都只用固定内存。

## 已取得的证据

- 流式读取9组测试：分块边界1–67字节、Unicode/转义、被跳过字段语法错误、错哈希/大小、路径替换、元数据与选择范围、确定性对象对照和规模测试。
- 规模夹具494,927,962字节（440MiB跳过 + 32MiB保留），128MiB V8堆限制，进程maxRSS=219,212KiB。临时生成的夹具已清理，不涉及用户数据。
- 生产只读stdin验证：Node22、V8堆256MiB、generation=g-488bff86d1fcd64e2ccea8801321f42bb3bebc8cc4a8285cc86c50f90d63b8bd，读取428,497,267字节，用时14,007ms，maxRSS=89,812KiB。完整SHA256=8e61cf660e5eea97444464d4007a8c52852cfcec98b3737e55987f2a3a2f4fc2。
- 该代只有updatedAt/retentionDays被选中，没有独立公开推荐账本/私有证据字段。不会从其他快照补造这些历史证据。
- SQLite增量/克隆198项通过，验证实际参考索引和原平局方向仍一致、基础表/赛果覆盖层/导出时间不变。
- 代际事务89项、代际端到端99项、参考索引18项、公开参考证据23项通过。
- 发布就绪新增强制规模/内存检查；不能用跳过规模测试的环境变量得到通过结果。

以上不是完整生产克隆写入与部署耗时证明；新签名包还须通过固定入口、最终标记、数据库/服务同代、恢复状态、受保护路径及采集完整周期验收。

## 同批复盘 UI

合并独立 UI 分支后包含正式/参考/影子分类、7/30天完整账本窗口、HAD/HHAD/UNKNOWN逐日与累计对账、默认HAD玩法切换、缺失版本/基准/覆盖率提示，以及移动端可键盘操作的折叠与清晰布局。详情与复现见 `review-evidence-dashboard.md`。不保证或改写命中率，新模型仍须独立前瞻门槛。

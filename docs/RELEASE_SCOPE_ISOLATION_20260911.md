# r727 发布中止与校验隔离修复

## 实际发布结果

2026-09-11 的 r727 整包 SHA256 为 `24497c3d7059b9a80f77fd28ce3b77d784448742fe6a128903ad10a97cfe1783`。远端事务于 11:24:29Z 开始，11:43:53Z 在最终 readiness 校验中失败，尚未交换应用目录。该包不能重放；后续必须使用新提交、新签名包及新鲜发布预检。

失败来自 `verifyDataValidationScopes.cjs`：预设归档数量为 14，实际读取值为 21。这不是测试项目数量变化。基础修复将校验器的 JSON 读取改为导入流式读取模块，测试原先只模拟直接的 `fs.readFileSync`，导入模块逃逸了模拟边界，读到了真实候选文件，也可能绕过测试对非法比分或私有归档的变更。不能将断言改成 21，也不能跳过该验收。

本次事务中的归档迁移、两轮 generation/datastore 生成以及两次 deadline capture 已通过；大快照读取未再次触发本次失败。远端总事务约 19 分 24 秒，此时间不代表成功发布耗时。

## 修复与验证

- 测试同时隔离导入的流式读取器；预设文件、变更后的 sync-meta 和私有归档使用相同模拟文件系统。
- 两次完整公共文件校验仍使用真实流式读取器，要求存在真实读取记录。
- 保留原有 70 项检查、201 次预设读取、46 个严格赛果场景和两次完整公共文件校验。
- readiness 新增隔离边界证据检查，签名前发布契约实际执行同一测试，并确认缺失隔离证据或真实读取时拒绝。

Windows Node 22 实际测试通过，70 项检查、229 次隔离读取、12 次真实读取。Linux 线上只读沙盒复核通过，70 项检查、229 次隔离读取、6 次真实读取。实际文件数量因平台部署布局不同而不同；不以放宽业务断言解决。Linux 校验源码仅在内存执行，运行用户为 football，文件系统只读，网络隔离，生产写入为 0。

完整签名前契约 341 项全部通过，ESLint 及 `git diff --check` 通过。证据：

- `outputs/r727-scope-contract-fix-verification.json`
- `outputs/scope-fixture-fix-linux-1789127423153.json`
- `outputs/full-r727-stage-timings-1789126797569.json`

## 失败后的线上完整性

11:49Z 的复核确认：当前完整运行包仍为 r718 (`8c28ca0f4a5b6094b633c4ac5418becee18b05c2d9937a583b3479405b5353b8`)，前端仍为 r719，已接受的基础修复补丁为 `46a94ee2a780b3888dc59be78d35f23dfc7275e0b577695f02aa328858394a5b`。12 个补丁文件哈希匹配，服务、数据新鲜度、快速赛果及推荐投影检查通过，可信采集器 2/2，`recoveryPending=false`。

11:50:35Z 将最新不可变 generation 与原始签名修复证据逐项比较，原有 635 条冻结记录、198 条可变推荐决定及其 198 条证据全部保留。当前记录增长为 636 条冻结记录、209 条决定和证据。证据 `outputs/original-repair-evidence-after-full-1789127435972.json` 的文件名不表示整包已上线，应以内容绑定的旧 runtime SHA 为准。

模型仍为参考／影子：`modelRiskStable=false`、`recommendationReliable=false`。该次修复不构成模型晋级证据。

## 下一步与停止边界

11:54:41Z 的新只读预检返回 `transition-window-closed`。下一比赛状态变化为 14:00Z（北京时间 22:00），正常流程要求 9440 秒窗口，最晚开始准备时间 11:22:40Z 已过。下一候选窗口为 **2026-09-12 03:00 北京时间**，必须届时重新预检，不能将候选时间当作预授权或预约。证据 `outputs/continuation-window-1789127682011.json`；没有占用新版本号、上传新包或创建租约。

后续从已合并的修复提交生成新整包，先核对源码和本地一致性克隆，再执行窗口、签名、上传前检查；获取新鲜冻结／推荐基线后仅部署一次。整包成功后还必须校验真实 runtime SHA、正式 Worker 周期和原有证据。

PostgreSQL-only 未切换，SQLite 导出仍参与正常发布。独立分支 `codex/native-publisher-20260911` 的数据库原子改名原语已有 8 项真实 PostgreSQL 测试，但完整签名发布器、数据库与 generation 指针的跨步骤恢复仍未验收。不得据此直接更改线上数据库或关闭 SQLite 导出。原始备份、失败包和恢复记录保留。

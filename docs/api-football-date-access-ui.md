# 日期权限、球队映射与数据缺口分开展示

2026-09-08。Q2 诊断与页面优化。本批新增页面/采集诊断不在已签名的 r704 内，不能当作已上线。

## 真实缺口重放

06:43:53 对主服务器完整43场当前 SQLite、既有 API 标准化赛程缓存及实体注册表进行同代只读对照。比较线上旧评分器与隔离候选评分器，无网络请求，禁止把旧缓存设为 live，也未更新注册表。

- 33场没有同日缓存，同时落在既有日期限制范围之外。当时记录的窗口为9月6日至8日，另外33场的查询日期为9日至11日。该记录不是新查询或永久套餐承诺。
- 其余10场共享9月8日的193条缓存赛程。旧评分器没有达到研究重放所设高分阈值的候选；候选评分器有4场：卡利亚里／莱切、乌迪内斯／拉齐奥、赫塔费／维戈塞尔塔、埃尔切／皇家社会，队名与综合分均为1。
- 4场仅是匹配候选，不是4场新核准映射。当前缓存映射核准仍为0；未来仍须真实当前周期来源响应、完整实体校验和各项接收时间门禁。不能将已过截止时间后取得的信息回填原推荐。

回取报告 `outputs/live-api-mapping-replay-1788821033489.json`，SHA-256 `77970976c969272c2d3de6811f528d2e2eb482b4d45b8d613381458a4b63113e`。该报告保留原/候选5个相关源码hash、原始缓存及注册表hash、完整每场分数和第二候选。06:55:42独立核对这5个候选文件与冻结r704文件的LF规范化hash完全相同；原始字节受换行格式影响，不能据此代替签名包身份验证。本批新增诊断会改变后续采集器源码，但没有再修改匹配权重或扩充别名。

最初探针发现旧线上没有新别名模块，记录为旧版本缺失而未创建该文件；另一次读取恰逢代际交接，被一致性断言拒绝。上面的成功报告来自随后同代/前后指针及缓存hash不变的重新读取，不使用失败探针的部分结果。

## 已实现的采集与 UI 改动

实际 API 合并流程为每场加入有界 fixtureAccess 诊断：查询日期、已记录范围、限制记录时间、诊断时间、有效时长和枚举状态。即使没有旧 API 信号，也能记录本场的日期限制；不创造核准映射或伤停/首发/赔率数据。

状态区分：超出记录范围、在记录范围内但未保证取得数据、账户受限、记录过期、记录无效。私有异常字符串和凭据不进入公开投影；日历、范围、时钟倒序及有效时长不合规时不显示“可查”。公开二次投影重新计算状态，不相信传入的状态标签。旧限制不存在时清除遗留诊断，不刷新上游来源记录时间。

页面在“补充源采集记录”内新增独立提示卡，把权限问题与映射状态分开。手机日期不拆行，查询日和范围分行展示；中英文说明强调“未取得伤停／首发不代表没有伤停／已确认首发”。不加入冻结决策的缺口计数，不改变已公开方向、模型权重或推荐概率。

## 验收

- 43项诊断测试（其中19项新增日期权限检查）通过，覆盖真实合并代码块、空旧信号、去敏、错误状态重算、API列表二次投影及隔离数据存储回读。
- 63项实际数据采用规则/TSX渲染检查通过，包含日期提示不改变冻结采用记录与计数。
- 原API hardening149项、现有比赛形状74项、时钟57项通过，网络调用0；不是生产重新抓取。
- 发布合同159项、部署配置122项、相关lint/diff、TypeScript构建及Vite生产构建通过。readiness现在要求43项总检查、19项权限检查及生产写入0，不降低原门禁。
- 06:54:16真实Edge/Chromium执行320／390／768／1440、中英文、三种合成消费者场景，共48项折叠／键盘展开检查通过；无页面溢出、关键数字截断或运行错误。人工查看390／768／1440截图，修正了首次手机截图的日期中间换行问题。

浏览器报告 `outputs/data-adoption-browser-report.json`，SHA `5a014982fdf77ec05535e5c2666dca411cd3b46598a815bf310ba60a4ee15870`；截图 `outputs/collector-diagnostics-390.png`、`-768.png`、`-1440.png`。旧5197预览未显示新预编译模块，保留该进程，在独立5273 Node22预览强制更新依赖后复验；验收后已关闭本轮5273会话。没有重启生产服务。构建仍有既有worldcup-glory-hero.jpg运行时资源警告，不宣称已修复。

本批没有执行新的Linux生产式完整readiness/PG验收，也没有部署。06:54:58 r704原队列仍实际存活，未开始，双marker仍旧r699；07:01单次尝试/07:06开始期限不变。整体仍需实际发布、真实来源核准与输入采用、完整页面上线验证及Q1–Q5剩余工作，不能用本批测试或4个候选宣称正式推荐和命中率已经改善。

# r693 准备：参考索引策略迁移与固定历史研究

本文件为候选改动说明，不是 r693 已上线证明。

## r692 失败证据

r692 固定签名入口从 2026-09-07 11:48:51 到 12:17:35 北京时间，最终 failed/exitCode=1，未执行原子切换。候选完整验收、原服务两次候选 barrier 恢复、生产副本复制和迁移检查均通过；最后隔离副本导出因 `SQLITE_ACTIVE_GENERATION_FAST_PATH_REQUIRED: warehouse-policy-mismatch` 被拒绝。两个 live marker 仍为 r687。

只读核对生产 `schema_meta`：基础 schema 是 `football-sqlite-v2-incremental`，预测身份版本 `prediction-state-v3`，warehouse policy 为 v3。新代码 v5 相比旧 v3 仅新增独立参考档案和索引投影合同。发布原来强制“策略完全一致”的快速导出，未包含这次确定的升级路径。

## 修复边界

- 只在发布明确设置只读源指针、要求快路径且显式允许参考策略升级时接纳精确 v3 -> v5。
- 克隆路径/文件身份、schema、预测身份、代际五字段、快赛果校验戳、额度与全部旧策略字段仍逐项一致；未知字段、类型差异及其他旧版本不接纳。
- 在事务中再次核对原文件与原策略，读取本代不可变快照，真实构建参考档案及成员证明索引，然后写入新策略。不是只修改版本号。
- 不重放比赛、赔率或预测基础表，不覆盖快赛果层，不改原导出时间，不回写生产源指针。原规范写锁、指针提交锁、失败回滚与发布完成验证不变。
- 升级后再次执行为精确 no-op。未知不匹配仍拒绝，不回退为无界全量导出。

## 本地验收

SQLite 增量/真实副本回归 194 项通过，新增场景实际从 v3 建出可验证索引并保留原平局方向；比赛、赔率、预测和私有模型表内容逐项一致，拒绝无授权升级/未知版本/额外字段/错误类型。发布事务 63/63、部署配置、Bash 语法和 diff 检查通过。真实生产规模的副本升级与下一次完整签名发布仍须另验。

## 研究改动

固定 A/B/C 的日期隔离、相同分母、缺失审计、7 日日期块区间、三组消融和复跑报告独立实现，不改变默认模型、生产权重或推荐门槛。详见 `docs/fixed-abc-historical-research.md`。历史结果未证明新模型优势，不据此晋级。

## 发布仍须完成

新序号签名、候选与生产规模验证、两个完成标记、recoveryPending=0、实际服务与数据库同代、worker 完整新周期、受保护路径及页面检查全部通过后才能称上线；不得重试 r692。

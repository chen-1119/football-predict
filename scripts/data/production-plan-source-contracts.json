{
  "version": "production-plan-required-source-contracts-v1",
  "required": [
    "versioned v1 public API routes",
    "protected recommendation reads",
    "admin bearer only and query token denied by tests",
    "public model evaluation is redacted",
    "recommendation boundary retries only across model-risk publication drift",
    "SQLite WAL schema has four incremental snapshot tables",
    "odds state persistence and JSONL maintenance stay bounded",
    "official odds receipt trail prevents backtest capture-bucket lookahead",
    "recommendation selection chronology is epoch ordered and invalid time fails closed",
    "sync data large JSON publication is bounded and atomic",
    "relay current, recent-result and archive freshness are independently timestamped",
    "match lifecycle is monotonic and reschedule-safe",
    "current list expires stale unresolved rows without fabricating results",
    "server reads from SQLite with fallback visibility",
    "UEFA official result supplement is strict, auditable, and archive-safe",
    "official club result supplement is allowlisted, tamper-evident, and archive-safe",
    "relay result publication is independent and monotonic",
    "independent relay lane uploads are serialized and rollback-safe",
    "runtime status is bounded and deep diagnostics are explicit",
    "v1 health merges sync worker heartbeat",
    "runtime monitor auto-repairs SQLite read drift",
    "official K League standings and results refresh before rebuild with timezone and cutoff safeguards",
    "runtime monitor detects strict official-result settlement misses",
    "candidate temporal admin diagnostics stay bounded across both production gates",
    "server processing is cloud-primary while official-source redundancy is runtime-evidenced",
    "local cloud sync fallback is opt-in only",
    "Sporttery egress WAF diagnostic exists",
    "overseas server is relay-first for Sporttery",
    "Sporttery relay proxy preflight is one command",
    "Sporttery relay proxy config is safe and one-command",
    "Sporttery relay collector clears stale failure after success",
    "Sporttery relay keeps full archive and minute fast lane in independent files",
    "Sporttery relay server enforces independent atomic full and fast uploads",
    "cloud sync fallback can export SQLite to production var-lib store",
    "large static payloads are disabled and stripped from dist",
    "cloud data push does not republish large dist payloads",
    "cloudflare scheduling worker does not serve protected data",
    "v1 reads use ETag and short TTL caches",
    "release restart transport failures use a bounded truthful maintenance fallback",
    "IP TLS bootstrap and strict readiness are reproducible",
    "performance smoke gate exists",
    "time-ordered rolling backtest and leakage guards",
    "market, Elo, Poisson, and historical blend candidates exist",
    "release mirrors public model artifacts into store",
    "LLM is limited to risk review and explanation",
    "post-cutoff predictions are locked and audited",
    "source fallback keeps stale data instead of empty publishes",
    "independent fast-file Sporttery relay keeps live freshness",
    "frontend consumes v1 APIs and exposes current status and recommendation contracts",
    "frontend timeout, abort, and retained-history regressions block production",
    "safe release script aborts before swap and rolls back after swap",
    "bundle release path supports uncommitted candidate packages",
    "release freeze keeps the candidate deadline heartbeat live and fail-closed",
    "release prebuilds SQLite online and activates it only after post-freeze CAS",
    "cold release recovery is fixed, directional, and behaviorally tested",
    "sudo automation is limited to signed release and fixed relay promoters",
    "offline release kit supports console recovery",
    "signed release SSH pins one console-verified host key",
    "release watch can wait for ssh recovery window",
    "remote public readiness verifies live cutover state",
    "candidate readiness separates unpublished markets from published atomic-chain gaps",
    "candidate cutoff watcher enforces settled 500-row and 5-of-6 window goal",
    "remote refresh readiness proves the protected result-to-review pipeline",
    "remote list detail and frozen archive share one recommendation direction",
    "release status reports deploy blockers",
    "api contract gate self-starts local protected server",
    "production verification runs the future-only temperature-neutralization suite gate",
    "production verification gates the Huawei FunctionGraph independent collector artifact",
    "production verification runs recommendation selection epoch chronology before readiness",
    "production verification enforces auditable recommendation confidence payload semantics",
    "fast result generation isolates receipt reviews and ships its exact contract",
    "postgres semantic result-only canonicalization is verified locally and inside signed candidates",
    "signed release uses a fail-closed dynamic transition lease around candidate verification"
  ]
}
